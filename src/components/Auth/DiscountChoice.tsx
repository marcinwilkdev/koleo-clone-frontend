import React, { Fragment } from "react";
import classes from "./styles/DiscountChoice.module.css";

interface Props {}

const DiscountChoice: React.FC<Props> = ({}) => {
    return (
        <Fragment>
            <div>Discount</div>
            <div>Discount</div>
        </Fragment>
    );
};

export default DiscountChoice;
